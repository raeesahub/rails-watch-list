
<h1><%= @list.name %> </h1>

<% @list.bookmarks.each do |bookmark| %>
<li><%= bookmark.movie.title %>: <%= bookmark.comment %><%= link_to 'Remove', bookmark_path(bookmark), method: :delete %> </li>
<% end %>

<%= link_to 'Back to all lists', lists_path  %>
